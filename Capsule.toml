[package]
name = "astrid-capsule-hook-bridge"
version = "0.1.0"
description = "Hook bridge capsule — maps lifecycle events to semantic hooks with merge strategies"
authors = ["Astrid Core Team"]
astrid-version = ">=0.1.0"

[[component]]
id = "hook-bridge"
file = "astrid_capsule_hook_bridge.wasm"
type = "executable"

# ── Session lifecycle ──
[[interceptor]]
event = "astrid.v1.lifecycle.session_created"
action = "on_session_created"

[[interceptor]]
event = "astrid.v1.lifecycle.session_ended"
action = "on_session_ended"

# ── Tool hooks ──
[[interceptor]]
event = "astrid.v1.lifecycle.tool_call_started"
action = "on_tool_call_started"

[[interceptor]]
event = "astrid.v1.lifecycle.tool_call_completed"
action = "on_tool_call_completed"

[[interceptor]]
event = "astrid.v1.lifecycle.tool_result_persisting"
action = "on_tool_result_persisting"

# ── Message hooks ──
[[interceptor]]
event = "astrid.v1.lifecycle.message_received"
action = "on_message_received"

[[interceptor]]
event = "astrid.v1.lifecycle.message_sending"
action = "on_message_sending"

[[interceptor]]
event = "astrid.v1.lifecycle.message_sent"
action = "on_message_sent"

# ── Sub-agent hooks ──
[[interceptor]]
event = "astrid.v1.lifecycle.sub_agent_spawned"
action = "on_subagent_spawned"

[[interceptor]]
event = "astrid.v1.lifecycle.sub_agent_completed"
action = "on_subagent_completed"

[[interceptor]]
event = "astrid.v1.lifecycle.sub_agent_failed"
action = "on_subagent_failed"

[[interceptor]]
event = "astrid.v1.lifecycle.sub_agent_cancelled"
action = "on_subagent_cancelled"

# ── Context compaction ──
[[interceptor]]
event = "astrid.v1.lifecycle.context_compaction_started"
action = "on_compaction_started"

[[interceptor]]
event = "astrid.v1.lifecycle.context_compaction_completed"
action = "on_compaction_completed"

# ── Kernel lifecycle ──
[[interceptor]]
event = "astrid.v1.lifecycle.kernel_started"
action = "on_kernel_started"

[[interceptor]]
event = "astrid.v1.lifecycle.kernel_shutdown"
action = "on_kernel_shutdown"

[capabilities]
# Hook bridge needs to publish hook results on the event bus
ipc_publish = [
    "hook.v1.result.*",
]
